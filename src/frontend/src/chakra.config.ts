import { extendTheme } from "@chakra-ui/react";

const registroFazendaTheme = extendTheme({
    fonts: {
        body: `'Roboto', sans-serif`,
        headings: `'Roboto', sans-serif`,
    },
})

export default registroFazendaTheme